<!-- Admin Navbar -->
<nav class="bg-white shadow-lg sticky top-0 z-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
            <div class="flex">
                <div class="flex-shrink-0 flex items-center">
                    <img class="h-12 w-auto" src="/krushiyuga_logo.png" alt="Krushiyuga">
                </div>
                <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
                    <a href="/admin/dashboard"
                        class="<%= pageTitle.includes('Dashboard') ? 'border-green-500 text-gray-900' : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700' %> inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium">
                        <i class="fas fa-chart-line mr-2"></i> Dashboard
                    </a>
                    <a href="/admin/products"
                        class="<%= pageTitle.includes('Products') ? 'border-green-500 text-gray-900' : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700' %> inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium">
                        <i class="fas fa-box mr-2"></i> Products
                    </a>
                    <a href="/admin/inquiries"
                        class="<%= pageTitle.includes('Inquiries') ? 'border-green-500 text-gray-900' : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700' %> inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium">
                        <i class="fas fa-envelope mr-2"></i> Inquiries
                    </a>
                </div>
            </div>
            <div class="hidden sm:ml-6 sm:flex sm:items-center sm:space-x-4">
                <a href="/" class="text-gray-500 hover:text-gray-700">
                    <i class="fas fa-home mr-1"></i> Visit Site
                </a>
                <div class="flex items-center">
                    <span class="text-gray-700 mr-2">
                        <i class="fas fa-user-circle mr-1"></i>
                        <%= admin.firstName %>
                            <%= admin.lastName %>
                    </span>
                    <a href="/admin/logout" class="text-red-600 hover:text-red-800">
                        <i class="fas fa-sign-out-alt"></i> Logout
                    </a>
                </div>
            </div>
            <!-- Mobile menu button -->
            <div class="flex items-center sm:hidden">
                <button type="button" onclick="toggleMobileMenu()"
                    class="inline-flex items-center justify-center p-2 rounded-md text-gray-400 hover:text-gray-500 hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-green-500">
                    <i class="fas fa-bars"></i>
                </button>
            </div>
        </div>
    </div>

    <!-- Mobile menu -->
    <div class="sm:hidden hidden" id="mobileMenu">
        <div class="pt-2 pb-3 space-y-1">
            <a href="/admin/dashboard"
                class="<%= pageTitle.includes('Dashboard') ? 'bg-green-50 border-green-500 text-green-700' : 'border-transparent text-gray-500 hover:bg-gray-50 hover:border-gray-300 hover:text-gray-700' %> block pl-3 pr-4 py-2 border-l-4 text-base font-medium">
                <i class="fas fa-chart-line mr-2"></i> Dashboard
            </a>
            <a href="/admin/products"
                class="<%= pageTitle.includes('Products') ? 'bg-green-50 border-green-500 text-green-700' : 'border-transparent text-gray-500 hover:bg-gray-50 hover:border-gray-300 hover:text-gray-700' %> block pl-3 pr-4 py-2 border-l-4 text-base font-medium">
                <i class="fas fa-box mr-2"></i> Products
            </a>
            <a href="/admin/inquiries"
                class="<%= pageTitle.includes('Inquiries') ? 'bg-green-50 border-green-500 text-green-700' : 'border-transparent text-gray-500 hover:bg-gray-50 hover:border-gray-300 hover:text-gray-700' %> block pl-3 pr-4 py-2 border-l-4 text-base font-medium">
                <i class="fas fa-envelope mr-2"></i> Inquiries
            </a>
            <div class="border-t border-gray-200 pt-4 pb-3">
                <div class="flex items-center px-4">
                    <div class="flex-shrink-0">
                        <i class="fas fa-user-circle text-2xl text-gray-400"></i>
                    </div>
                    <div class="ml-3">
                        <div class="text-base font-medium text-gray-800">
                            <%= admin.firstName %>
                                <%= admin.lastName %>
                        </div>
                        <div class="text-sm font-medium text-gray-500">
                            <%= admin.email %>
                        </div>
                    </div>
                </div>
                <div class="mt-3 space-y-1">
                    <a href="/"
                        class="block px-4 py-2 text-base font-medium text-gray-500 hover:text-gray-800 hover:bg-gray-100">
                        <i class="fas fa-home mr-2"></i> Visit Site
                    </a>
                    <a href="/admin/logout"
                        class="block px-4 py-2 text-base font-medium text-red-600 hover:text-red-800 hover:bg-gray-100">
                        <i class="fas fa-sign-out-alt mr-2"></i> Logout
                    </a>
                </div>
            </div>
        </div>
    </div>
</nav>

<script>
    function toggleMobileMenu() {
        const mobileMenu = document.getElementById('mobileMenu');
        mobileMenu.classList.toggle('hidden');
    }
</script>